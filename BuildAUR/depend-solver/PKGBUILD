# Maintainer: MoYingJi <moyingjiaw@outlook.com>

pkgname=depend-solver
pkgver=1
pkgrel=1
arch=("any")
source=("pkgs.txt")
sha256sums=("SKIP")

provides=()

for e in $(cat "pkgs.txt"); do
	provides+=("$e")
done

package() {
	echo Depend Solver
}

